<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MauiApp1.ViewModels"
             xmlns:models="clr-namespace:MauiApp1.Models"
             x:Class="MauiApp1.Views.TransactionsView"
             x:DataType="viewmodels:TransactionsViewModel"
             Title="Transaction History">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,20"
            Spacing="10">
            
            <Label
                Text="Transaction History"
                FontSize="24"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Margin="0,0,0,20" />
            
            <CollectionView
                ItemsSource="{Binding Transactions}"
                SelectionMode="None">
                
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Transaction">
                        <Grid
                            Padding="15,10"
                            ColumnDefinitions="*,Auto"
                            RowDefinitions="Auto,Auto,Auto">
                            
                            <Label
                                Grid.Row="0"
                                Grid.Column="0"
                                Text="{Binding Description}"
                                FontSize="16"
                                FontAttributes="Bold"
                                VerticalOptions="Center" />
                            
                            <Label
                                Grid.Row="0"
                                Grid.Column="1"
                                FontSize="16"
                                FontAttributes="Bold"
                                VerticalOptions="Center"
                                Text="{Binding Amount, StringFormat='{0:+#,##0.00;-#,##0.00;0.00} RSD'}" />
                            
                            <Label
                                Grid.Row="1"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                Text="{Binding Date, StringFormat='{0:dd.MM.yyyy HH:mm}'}"
                                FontSize="12"
                                TextColor="Gray"
                                Margin="0,5,0,0" />
                            
                            <BoxView
                                Grid.Row="2"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                HeightRequest="1"
                                Color="LightGray"
                                Margin="0,10,0,0" />
                            
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                
            </CollectionView>
            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
